
/** Initializes a new Goal game and starts running it. */
class Main {
    function void main() {
        var GoalGame game;
        let game = GoalGame.new();
        do game.displayWelcomeScreen();
        do game.run();
        do game.dispose();
        return;
    }
}
